<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="FutureMindset.Dashboard" >
	<ContentView.Content >
<!--			Source="imageWallPaper.jpg"-->
	 <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <Image Source="originalWallPaper.jpg" AbsoluteLayout.LayoutBounds="0,0,1,1"
         AbsoluteLayout.LayoutFlags="All" Aspect="AspectFill"/>
        <ScrollView AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All">
		<Grid  AbsoluteLayout.LayoutBounds="0,0,1,1" >
		<BoxView Color="Black" Opacity="0.6" AbsoluteLayout.LayoutBounds="0,0,1,1" />	
         <StackLayout Orientation="Vertical"   VerticalOptions="Center"  HorizontalOptions="Center"  x:Name="SwitchboardColour" >

        <Image x:Name="SwitchboardIcon" 
               HorizontalOptions="Center" 
               Source="{Binding Icon}"			   
			   VerticalOptions="Center"  />

        <Label x:Name="SwitchboardLabel"
			   Margin="{Binding Margins}"
               Text="{Binding Label}"
               TextColor="White" 				   
			   VerticalOptions="Center" 
			   HorizontalOptions="Center"
			   FontAttributes="Bold" />
      
    		</StackLayout>
			</Grid>
        </ScrollView>
    </AbsoluteLayout>

	</ContentView.Content>
</ContentView>
